package util;

import entity.Wallet;

public class WalletDTOConverter {
    public class WalletUtil {
        public static void updateWalletBalance(Wallet wallet, double amount) {
        }
        public static boolean hasSufficientBalance(Wallet wallet, double amount) {  //Used to check if the wallet has enough balance
            return false;
        }
    }
}
